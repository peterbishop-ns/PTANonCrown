<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PTANonCrown.LiveTreePage"
             xmlns:viewModels="clr-namespace:PTANonCrown.ViewModel"
             xmlns:models="clr-namespace:PTANonCrown.Data.Models;assembly=PTANonCrown.Data"
             xmlns:converter="clr-namespace:PTANonCrown.Converter"
             xmlns:behav="clr-namespace:PTANonCrown.Behaviours"
             BackgroundColor="#F0F0F0"
             Title="Live Trees">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:SpeciesConverter x:Key="SpeciesConverter" />
            <converter:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>

        <VerticalStackLayout VerticalOptions="Start"
                             Padding="5"
                             Spacing="5">
            <Label Text="{Binding ErrorMessage}"
                   Background="Pink"
                   IsVisible="{Binding ErrorMessage, 
     Converter={StaticResource StringToVisibilityConverter}}" />
            <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="10">
                
                <!-- STAND/PLOT -->
                <Border Grid.Row="0"
                        BackgroundColor="LightGoldenrodYellow"
                        StrokeShape="RoundRectangle 2,2,2,2"
                        WidthRequest="200">
                    <HorizontalStackLayout HorizontalOptions="Center">
                        <Label Text="Stand"
                               FontAttributes="Bold"
                               Margin="10,0,0,0" />
                        <Label Text="{Binding CurrentStand.StandNumber}"
                               Margin="3,0,0,0" />
                        <Label Text="Plot"
                               FontAttributes="Bold"
                               Margin="10,0,0,0" />
                        <Label Text="{Binding CurrentPlot.PlotNumber}"
                               Margin="3,0,0,0" />
                    </HorizontalStackLayout>
                </Border>



                <Grid Grid.Row="1"
                    RowDefinitions="Auto,Auto"
                      ColumnSpacing="10"
                      ColumnDefinitions="200,Auto,Auto,Auto">
                    <HorizontalStackLayout Grid.Row="0"
                                           Grid.Column="0">
                        <Label Text="Include Biodiversity"
                               Grid.Row="0"
                               Grid.Column="0" />
                        <ImageButton Source="info.png"
                                     HorizontalOptions="Center"
                                     Style="{StaticResource ImageButtonHelper}"
                                     Command="{Binding ShowHelperTextCommand}"
                                     CommandParameter="IncludeBiodiversity" />
                    </HorizontalStackLayout>

                    <CheckBox x:Name="IncludeBiodiversity"
                              IsChecked="{Binding IsCheckedBiodiversity, Mode=TwoWay}"
                              Grid.Row="0"
                              Grid.Column="1" />
                    
                    
                    <Label Text="Number of Trees In Plot"
                           Grid.Row="1"
                           VerticalOptions="Center"
                           Grid.Column="0"/>
                    <Entry Text="{Binding CurrentPlot.TreeCount, Mode=TwoWay}"
                           Style="{StaticResource EntryParent}"
                           Grid.Row="1"
                           Grid.Column="1"
                           WidthRequest="50"></Entry>

                    <Button Text="Refresh"
                            Grid.Row="1"
                            Grid.Column="2"
                            Command="{Binding SpecifyTreeCountInPlotCommand}" />

                    <Button Text="Use Predicted Heights"
                            Grid.Row="1"
                            Grid.Column="3"
                            Command="{Binding UsePredictedHeightsCommand}" />
                </Grid>
              
            </Grid>

            <CollectionView ItemsSource="{Binding CurrentPlot.PlotTreeLive}">
                <CollectionView.Header>
                    <Grid ColumnDefinitions="100,200,80,80,100,60,60,60,60,80,80,80,60,60,60"
                          BackgroundColor="LightGray"
                          Padding="5">
                        <Label Grid.Column="0"
                               Text="Tree #"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="1"
                               Text="Species"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="2"
                               Text="DBH (cm)"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="3"
                               Text="Height (m)"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="4"
                               Text="Predicted"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="5"
                               Text="AGS"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="6"
                               Text="LIT"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="7"
                               Text="LT"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="8"
                               Text="Cavity"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="9"
                               Text="Diversity"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="10"
                               Text="Mast"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="11"
                               Text="Planted"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="12"
                               Text="In Situ"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Label Grid.Column="13"
                               Text="Ex Situ"
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                    </Grid>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="100,200,80,80,100,60,60,60,60,80,80,80,60,60,60"
                              Padding="5"
                              BackgroundColor="WhiteSmoke">

                            <!-- Tree Number -->
                            <HorizontalStackLayout Grid.Column="0" Spacing="5">
                                <Label Text="{Binding TreeNumber}"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center" />

                                <!-- Source="Resources/Images/delete_button.png"-->
                                <Button     Text="x"
                                            TextColor="Red"
                                            FontSize="24"
                                            BackgroundColor="Transparent"
                                            BorderWidth="1"
                                            CornerRadius="6"
                                            VerticalOptions="Center"
                                            WidthRequest="40"
                                            HeightRequest="40"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=DeleteLiveTreeCommand}"
                                            CommandParameter="{Binding .}" />

                            </HorizontalStackLayout>
                           
                            <!-- Species: Entry + Picker -->
                            <HorizontalStackLayout Grid.Column="1"
                                                   Spacing="5">
                                <Entry WidthRequest="50"
                                       Placeholder="e.g. bF"
                                       Text="{Binding SearchSpecies, Mode=TwoWay}" />
                                <Picker WidthRequest="400"
                                        ItemsSource="{Binding LookupTrees}"
                                        SelectedItem="{Binding TreeSpecies, Mode=TwoWay}"
                                        ItemDisplayBinding="{Binding Name}" />
                            </HorizontalStackLayout>

                            <!-- DBH -->
                            <Entry Grid.Column="2"
                                   Text="{Binding DBH_cm, Mode=TwoWay}"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />

                            <!-- Height -->
                            <Entry Grid.Column="3"
                                   Text="{Binding Height_m, Mode=TwoWay}"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />

                            <!-- Predicted Height (read-only) -->
                            <Label Grid.Column="4"
                                   Text="{Binding HeightPredicted_m, StringFormat='({0})'}"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />

                            <!-- AGS -->
                            <CheckBox Grid.Column="5"
                                      IsChecked="{Binding AGS, Mode=TwoWay}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- LIT (read-only from species) -->
                            <CheckBox Grid.Column="6"
                                      IsChecked="{Binding TreeSpecies.LIT}"
                                      IsEnabled="False"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- LT (read-only from species) -->
                            <CheckBox Grid.Column="7"
                                      IsChecked="{Binding TreeSpecies.LT}"
                                      IsEnabled="False"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- Cavity -->
                            <CheckBox Grid.Column="8"
                                      IsChecked="{Binding Cavity, Mode=TwoWay}"
                                      IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=IsCheckedBiodiversity}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- Diversity -->
                            <CheckBox Grid.Column="9"
                                      IsChecked="{Binding Diversity, Mode=TwoWay}"
                                      IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=IsCheckedBiodiversity}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- Mast -->
                            <CheckBox Grid.Column="10"
                                      IsChecked="{Binding Mast, Mode=TwoWay}"
                                      IsEnabled="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=IsCheckedBiodiversity}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- Planted visibility check -->
                            <CheckBox Grid.Column="11"
                                      IsChecked="{Binding Planted, Mode=TwoWay}"
                                      IsVisible="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=CurrentPlot.IsPlanted}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- In Situ -->
                            <CheckBox Grid.Column="12"
                                      IsChecked="{Binding PLInSitu, Mode=TwoWay}"
                                      IsVisible="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=CurrentPlot.IsPlanted}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />

                            <!-- Ex Situ -->
                            <CheckBox Grid.Column="13"
                                      IsChecked="{Binding PLExSitu, Mode=TwoWay}"
                                      IsVisible="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=CurrentPlot.IsPlanted}"
                                      HorizontalOptions="Center"
                                      VerticalOptions="Center" />
     
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>